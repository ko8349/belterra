# Belterra v3 — Authentic Logo + Real Layouts

Corrected version:
- Uses the actual approved logo image, scaled properly.
- Does not use fake CSS logo.
- Does not use simplified inaccurate redrawn layouts.
- Uses your real architectural layout screenshots in premium brand frames.
- Keeps approved renders.

Run:
npm install
npm run dev
